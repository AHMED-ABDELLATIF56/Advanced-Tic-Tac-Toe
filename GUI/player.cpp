// AIPlayer.cpp

/*#include "player.h"
#include <cstdlib> // For rand()

Player::Player(Player type) : playerType(type) {}

int Player::makeMove(const std::vector<char> &board) {
    // Implement your AI logic here to generate a move
    int move;
    // Example: Randomly choose an empty cell
    do {
        move = std::rand() % 9; // Generate a random move (0-8)
    } while (board[move] != ' '); // Ensure the move is valid (empty cell)

    return move;
}
*/
